spring:
    jms:
        template:
            default-destination: library.books.queue
    artemis:
        host: localhost
        port: 61616
        user: davidepravata
        password: dp
        embedded:
            enabled: false
    datasource:
        url: jdbc:mysql://localhost:3306/spring?useSSL=false&&serverTimezone=Europe/Rome
        username: spring
        password: mf15mdj
        validation-query: select 1